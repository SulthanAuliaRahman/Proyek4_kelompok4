package com.example.healthsync.ui.theme

import androidx.compose.ui.graphics.Color

val BottomNavColor = Color(0xFFB9E5E8)  // Warna Bottom Navbar
val BackgroundColor = Color(0xFFFFFFFF) // Warna Background
val SelectedItemColor = Color(0xFFFFFFFF) // Warna Item yang dipilih (icon dan text)
val SelectedIndicator = Color(0xFFD3D3FF) // Warna Item yang dipilih (yang kotak indicator nya)
val UnselectedItemColor = Color.Gray // Warna Item yang tidak dipilih
